package com.thizzer.jtouchbar.item.view;

public enum TouchBarButton$ButtonType {
    MOMENTARY_LIGHT,
    PUSH_ON_PUSH_OFF,
    TOGGLE,
    SWITCH,
    RADIO,
    MOMENTARY_CHANGE,
    ON_OFF,
    MOMENTARY_PUSH_IN,
    ACCELERATOR,
    MOMENTARY_MULTILEVEL_ACCELERATOR;

    private static final TouchBarButton$ButtonType[] $VALUES = new TouchBarButton$ButtonType[]{
            MOMENTARY_LIGHT, PUSH_ON_PUSH_OFF, TOGGLE, SWITCH, RADIO, MOMENTARY_CHANGE, ON_OFF, MOMENTARY_PUSH_IN, ACCELERATOR, MOMENTARY_MULTILEVEL_ACCELERATOR
    };
}
